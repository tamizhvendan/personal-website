---
layout: base
---
<article class="mx-auto max-w-3xl">
  <div class="">
    <h1 class="text-center leading-normal">{{title}}</h1>
    <div class="flex space-x-1 text-sm mt-3 items-center justify-center w-full opacity-70">
      <dt>Published on</dt>
      <dd>
        <time datetime="{{ date | dateIso }}">{{ date | dateReadable }}</time>
      </dd>
      <dt class="sr-only">Read Time</dt>
      <dd>
        <span> · {{ content | timeToRead }} read </span>
      </dd>
    </div>
    <ul class="mt-3 flex items-center justify-center w-full flex-wrap gap-3">
      {%- for tag in tags | filterTagList %}
        {%- set tagUrl %}/tags/{{ tag | slugify }}/{% endset %}
        <li>
          <a href="{{ tagUrl }}" class="inline-flex items-center rounded-full bg-gray-100 px-2 py-1 text-sm font-medium text-gray-600 ring-1 ring-inset ring-gray-500/10">{{ tag }}</a>
        </li>
      {%- endfor %}
    </ul>
  </div>
  <div 
    class="mt-6 max-w-none
           prose md:prose-lg md:prose-xl
           prose-h2:font-medium prose-h2:text-3xl
           prose-img:mx-auto prose-img:shadow-lg prose-img:rounded
           prose-code:font-normal prose-pre:shadow-lg prose-pre:rounded">{{ content | safe }}</div>
</article>

<div class="max-w-xl mx-auto">
  <hr class="my-4 md:my-6 lg:my-8"/>

  <p class="text-center text-lg max-w-lg">
  Did the content capture your interest? Stay in the loop by subscribing to the <a class="underline" href="/feed.xml">RSS feed</a> and staying informed!
<p></div>

    <div class="my-4 md:my-6 lg:my-8">
      <script src="https://utteranc.es/client.js"
          repo="tamizhvendan/personal-website"
          issue-term="title"
          label="comment"
          theme="github-light"
          crossorigin="anonymous"
          async></script>
    </div>